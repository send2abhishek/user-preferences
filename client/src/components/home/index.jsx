import { Typography } from "@mui/material";
import React from "react";

const Home = () => {
  return (
    <div>
      <Typography variant="h1">index</Typography>
    </div>
  );
};

export default Home;
